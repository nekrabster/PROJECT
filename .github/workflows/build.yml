name: Build Windows EXE with Nuitka (Clang + LTO + Parallel)

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    # Используем Windows 2022 для гарантированного наличия Clang и MSVC Dev Cmd
    runs-on: windows-2022

    steps:
      - name: 🧾 Checkout code
        uses: actions/checkout@v4

      - name: 🐍 Setup Python 3.13 (кеш pip)
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: 'pip'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # Обязательно последняя версия Nuitka
          pip install --upgrade nuitka

      - name: 🧰 Setup MSVC Dev Cmd (Windows SDK)
        uses: ilammy/msvc-dev-cmd@v1

      - name: ⚙️ Install LLVM/Clang 17
        uses: egor-tensin/setup-clang@v1
        with:
          version: 17

      - name: 🔍 Find certifi cacert.pem
        id: certifi_path
        shell: bash
        run: |
          echo "certifi_path=$(python -c 'import certifi; print(certifi.where())')" >> $GITHUB_OUTPUT

      - name: 🔧 Build .exe with Nuitka
        shell: pwsh
        env:
          PYTHONIOENCODING: "utf-8"
          PYTHONLEGACYWINDOWSSTDIO: "utf-8"
          # Переключаемся на clang-cl вместо MSVC
          CC: "clang-cl"
          CXX: "clang-cl"
        run: |
          python -m nuitka main.py `
            --standalone `                         # автономный .exe
            --onefile `                            # упаковать всё в один .exe
            --clang `                              # использовать clang-cl
            --lto=yes `                            # link-time optimizations
            --jobs=$(Get-ComputerInfo | Select-Object -Expand CpuLogicalProcessorCount) `  
                                                  # параллельная компиляция по всем логическим ядрам
            --enable-plugin=pyqt6 `               # плагин для PyQt6
            --enable-plugin=data-files `          # автоподключение ресурсов
            --include-package=ui `                # все ваши модули UI
            --include-data-dir=icons=icons `      # папка с png/ico
            --include-data-file="${{ steps.certifi_path.outputs.certifi_path }}=certifi/cacert.pem" `
                                                  # сертификаты для HTTPS
            --windows-icon-from-ico=icon.ico `    # иконка exe
            --output-dir=dist `
            --output-filename=Soft-K.exe `
            --remove-output `                     # чистим dist перед сборкой
            --follow-imports `
            --assume-yes-for-downloads `
            --verbose                             # подробный лог

      - name: ☁️ Upload .exe as artifact
        uses: actions/upload-artifact@v4
        with:
          name: Soft-K.exe
          path: dist/Soft-K.exe
          retention-days: 3