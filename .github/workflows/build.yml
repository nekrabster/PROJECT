name: Build Secure .exe with Nuitka (Python 3.13 + MSVC + Optimized)

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: windows-latest
    env:
      PYTHONHASHSEED: 0   # для детерминированности сборки
      NUITKA_DISABLE_COPYRIGHT_WARNING: "1"

    steps:
      - name: ✅ Checkout code
        uses: actions/checkout@v4

      - name: 🗑️ Clean VCS artifacts
        run: |
          Remove-Item icons\.DS_Store, icons\Thumbs.db -ErrorAction SilentlyContinue
          git clean -fdx .

      - name: 🐍 Setup Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: 'pip'

      - name: 📦 Install dependencies securely
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install --no-build-isolation --require-hashes -r requirements.txt
          pip install nuitka==2.6.9

      - name: 🧾 Versions info
        run: |
          python --version
          python -m nuitka --version
          cl.exe

      - name: 🧠 Ensure enough disk
        run: dir C:\

      - name: 🔧 Setup MSVC environment
        uses: ilammy/msvc-dev-cmd@v1
        id: msvc

      - name: 🛡️ Build optimized standalone .exe with Nuitka
        shell: pwsh
        run: |
          # Сброс отладочных переменных
          Remove-Item Env:PYTHONDEBUG,Env:PYTHONDEVMODE,Env:DEBUG -ErrorAction SilentlyContinue

          $env:CL = '/MD /O2 /GS-'  # оптимизации и отключение безопасных стек-канарей (заменить, если нужен)

          python -m nuitka main.py `
            --onefile `
            --standalone `
            --follow-imports `
            --experimental=nf-only-bytecode-pgo `
            --enable-plugin=pyqt6 `
            --msvc=latest `
            --jobs=4 `
            --windows-icon-from-ico=icon.ico `
            --windows-company-name="SecureSoft" `
            --windows-product-name="Soft-K" `
            --windows-product-version=2.3.6 `
            --windows-console-mode=disable `
            --output-filename=Soft-K.exe `
            --include-data-dir=icons=icons `
            --remove-output `
            --include-module-dir=ui `
            --python-flag=-OO  # удаление docstrings, оптимизация
          
      - name: 📥 Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Soft-K.exe
          path: Soft-K.exe
          retention-days: 3

      - name: 🐞 Upload crash report (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: nuitka-crash-report
          path: compilation-report.xml
          retention-days: 3
