name: Сборка Windows EXE с Nuitka (Оптимизировано для скорости)

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: windows-2022

    steps:
      - name: 🧾 Загрузка кода
        uses: actions/checkout@v4

      - name: 🐍 Установка Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'  # Кэширование для ускорения установки пакетов

      - name: 🆙 Обновление pip
        run: python -m pip install --upgrade pip

      - name: 📦 Установка зависимостей
        run: pip install -r requirements.txt

      - name: 🌙 Установка последней версии Nuitka
        run: pip install --upgrade nuitka

      - name: 🧰 Настройка MSVC для Clang
        uses: ilammy/msvc-dev-cmd@v1

      - name: ⚙️ Установка LLVM (Clang)
        uses: egor-tensin/setup-clang@v1
        with:
          version: 17

      - name: 🛠 Сборка .exe с Nuitka и оптимизациями
        shell: pwsh
        env:
          PYTHONIOENCODING: "utf-8"
          PYTHONLEGACYWINDOWSSTDIO: "utf-8"
        run: |
          $env:CC = "clang-cl"
          $env:CXX = "clang-cl"
          python -m nuitka `
            main.py `
            --standalone `
            --onefile `
            --remove-output `
            --output-dir=dist `
            --output-filename=Soft-K `
            --windows-icon-from-ico=icons/icon.ico `
            --assume-yes-for-downloads `
            --follow-imports `
            --enable-plugin=pyqt6 `
            --enable-plugin=data-files `
            --lto=yes `
            --clang `
            --jobs=4 `
            --no-pyi-file `
            --windows-company-name="SecureSoft" `
            --windows-product-name="Soft-K" `
            --windows-product-version="1.0.0" `
            --include-data-files=icons/*.png=icons/ `
            --include-data-files=icons/*.ico=icons/

      - name: ☁️ Выгрузка .exe как артефакт
        uses: actions/upload-artifact@v4
        with:
          name: Soft-K.exe
          path: dist/Soft-K.exe
          retention-days: 3

      - name: 📋 Загрузка логов сборки (при ошибке)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build_logs
          path: |
            build/
            *.log
