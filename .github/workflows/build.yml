name: Build Windows EXE with Nuitka

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: 🧾 Checkout code
        uses: actions/checkout@v4

      - name: 🐍 Set up Python 3.13.5
        uses: actions/setup-python@v5
        with:
          python-version: '3.13.5'

      - name: 🆙 Upgrade pip
        run: python -m pip install --upgrade pip

      - name: 📦 Install requirements
        run: pip install -r requirements.txt

      - name: 🌙 Install latest Nuitka
        run: pip install --upgrade nuitka

      - name: 💿 Install Clang (optional, отключено)
        run: echo "Skipping Clang for speed optimization"

      - name: 🔍 Find certifi cacert.pem path
        id: certifi_path
        shell: bash
        run: |
          echo "certifi_path=$(python -c 'import certifi; print(certifi.where())')" >> $GITHUB_OUTPUT

      - name: 🛠 Build Soft-K.exe with Nuitka
        shell: pwsh
        run: |
          nuitka `
            main.py `
            --standalone `
            --onefile `
            --remove-output `
            --output-dir=dist `
            --output-filename=Soft-K `
            --windows-icon-from-ico=icon.ico `
            --windows-disable-console `
            --follow-imports `
            --nofollow-import-to=tests,setuptools,examples `
            --enable-plugin=pyqt6 `
            --enable-plugin=data-files `
            --enable-plugin=certifi `
            --include-data-file="${{ steps.certifi_path.outputs.certifi_path }}=certifi/cacert.pem" `
            --include-data-dir=icons=icons `
            --include-package=ui `
            --include-package=ui.* `
            --no-pyi-warnings `
            --static-libpython=yes `
            --jobs=4 `
            --low-memory-mode=no `
            --show-modules `
            --report=build-report.txt

      - name: ☁️ Upload Soft-K.exe
        uses: actions/upload-artifact@v4
        with:
          name: Soft-K.exe
          path: dist/Soft-K.exe

      - name: 📋 Upload build logs if failed
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build_logs
          path: |
            build/
            *.log
            build-report.txt